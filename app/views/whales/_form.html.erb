<%= form_for(whale) do |f| %>
  <% if whale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(whale.errors.count, "error") %> prohibited this whale from being saved:</h2>

      <ul>
      <% whale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
